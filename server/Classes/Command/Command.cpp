#include "Command.hpp"

string header_1(User user)
{
    string reply;

    reply = :localhost 001 dOD;

    return(reply);
}

string header_2(User user)
{
    string reply;
    
    reply = :dOD!dginisty@0 ;

    return(reply);
}